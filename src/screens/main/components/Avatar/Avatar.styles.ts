import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  circle: {
    backgroundColor: 'black',
  },
  circleContainer: {
    overflow: 'hidden',
  },
  lineSvg: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
  },
});
